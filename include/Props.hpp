#pragma once
#include <SFML/Graphics.hpp>

#define WINDOW_WIDTH (sf::VideoMode::getDesktopMode().width -  50 * sf::VideoMode::getDesktopMode().width/100)
#define WINDOW_HEIGHT (sf::VideoMode::getDesktopMode().height -  30 * sf::VideoMode::getDesktopMode().height/100)

#define size_tile_reg_height 21
#define size_tile_reg_width 12